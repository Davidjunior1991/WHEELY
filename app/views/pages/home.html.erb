<%# <div class="container"> %>
  <h1 class="welcome-title">Welcome to Wheely</h1>
  <p class="welcome-subtitle">"Get Rolling with Wheely!"</p>

  <%= link_to "Add a car", new_car_path, class: "btn-home" %>

  <div class="cars-container">
      <% @cars.each do |car| %>
<%= link_to car_path(car) do %>
    <div class="car-trip">
      <%= cl_image_tag car.photo.key %>
      <div class="car-trip-infos">

          <h2><%= car.name %></h2>

        <h2 class="car-trip-pricing">£ <%= car.price_per_day %></h2>


      </div>
    </div>
    <% end %>
        <% end %>
  </div>
<%# </div> %>
<div class="container-map">
<div style="width: 100%; height: 600px;"
  data-controller="map"
  data-map-markers-value="<%= @markers.to_json %>"
  data-map-api-key-value="<%= ENV['MAPBOX_API_KEY'] %>"></div>
  </div>
