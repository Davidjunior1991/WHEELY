
<%= @car.name %>
<%= cl_image_tag @car.photo.key, height: 300, width: 400, crop: :fill %>
<hr>
<% unless @car.user == current_user %>
  <%= link_to "create booking", new_car_booking_path(@car), class:"btn btn-success" %>
<% end %>
